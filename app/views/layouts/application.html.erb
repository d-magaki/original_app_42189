<!DOCTYPE html> 
  <head>
    <title>OriginalApp42189</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Flatpickr CDN -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
  </head>

  <body>
    <!-- 🔹 ヘッダー -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">
        <a class="navbar-brand" href="<%= root_path %>">案件管理</a>

        <% if user_signed_in? %>
          <span class="navbar-text">
            ようこそ、<%= current_user.user_name %> さん！ (社員ID: <%= current_user.employee_id %>)
          </span>
          <%= button_to "ログアウト", destroy_user_session_path, method: :delete %>
          <%= link_to "ユーザー一覧", users_path, class: "btn btn-secondary ml-3" %>

        <% else %>
          <%= link_to "ログイン", new_user_session_path, class: "btn btn-primary" %>
          <%= link_to "新規登録", new_user_registration_path, class: "btn btn-success ml-2" %>
        <% end %>
      </div>
    </nav>

    <!-- 🔹 メインコンテンツ -->
    <div class="container mt-4">
      <%= yield %>
    </div>

    <!-- 🔹 Flatpickr の設定 -->
    <script>
      document.addEventListener("DOMContentLoaded", function() {
        flatpickr("#order_date", { enableTime: false, dateFormat: "Y-m-d" });
        flatpickr("#due_date", { enableTime: false, dateFormat: "Y-m-d" });
      });
    </script>
  </body>
</html>